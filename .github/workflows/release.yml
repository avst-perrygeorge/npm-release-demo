name: Release SDK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      issues: write
    outputs:
      version: ${{ steps.release.outputs.version }}
    steps:
      - name: Check values
        run: |
          echo "repo: ${{ github.repository }}"
          echo "owner: ${{ github.repository_owner }}"
      - name: Generate token
        id: generate_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.PRIVATE_KEY }}

      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ steps.generate_token.outputs.token }}

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Fetch OpenAPI spec
        run: |
          curl -L --fail \
            https://salable.app/openapi.yaml \
            -o openapi.yaml

      - name: List OpenAPI spec
        run: |
          ls -la openapi.yaml
          cat openapi.yaml

      - name: Generate SDK with Kiota
        run: |
          echo "Generating SDK with Kiota from openapi.yaml"
          echo "Would run: npx kiota generate -l typescript -d openapi.yaml -o packages/sdk"

      - name: Commit for semantic release
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit --allow-empty -m "fix: openapi spec updated"

      - name: Release
        id: release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}
